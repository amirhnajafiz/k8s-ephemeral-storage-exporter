apiVersion: v1
kind: Service
metadata:
  name: {{ include "local-storage-metrics-exporter.fullname" . }}
  labels:
    {{- include "local-storage-metrics-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "local-storage-metrics-exporter.selectorLabels" . | nindent 4 }}
